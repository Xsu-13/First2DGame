using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class forSaving : MonoBehaviour
{
    [SerializeField] public int indexForSave;
    [SerializeField] public ScObjPotion PotionLink;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
